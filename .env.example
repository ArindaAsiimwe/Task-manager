# Database Configuration (Railway provides these automatically when you provision PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/taskmanager

# Application Configuration
PORT=3000
NODE_ENV=development

# API Key for secure access (set this in Railway's environment variables)
API_KEY=your-secret-api-key-here
